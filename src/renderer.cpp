#include "renderer.hpp"





// draw terminal
// takes pos, border style, size, terminal object/id 
// redraw every frame/every keypress/update to the read(master_fd) or write()

// seperate tiling manager - give it current pos and size of currently opened terminals and return a pos, size of where a new terminal would be
// use return values in draw_terminal();




